spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/usersDB
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9020
  servlet:
    context-path: /users

jwt:
  secretKey: djdiwnr45@$&)"?afk1!#@%^7)*NM>>!@%^&KJJjfshjf ewk124554uiou%^&*

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka