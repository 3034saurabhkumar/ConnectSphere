spring:
  application:
    name: post-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postsDB
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9010
  servlet:
    context-path: /posts

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
