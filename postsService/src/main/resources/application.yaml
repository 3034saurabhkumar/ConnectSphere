spring:
  application:
    name: postsService
  datasource:
    url: jdbc:postgresql://localhost:5432/postsDB
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9010
  servlet:
    context-path: /api/v1/posts

